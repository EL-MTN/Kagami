# Kansoku — Configuration

All knobs are env-driven. Template: [`apps/api/.env.example`](../apps/api/.env.example)
and [`apps/dashboard/.env.example`](../apps/dashboard/.env.example).

## API (`apps/api/.env`)

All env vars are declared in `apps/api/src/env.ts` (the `@kagami/env` spec —
schema + docs in one place) and read through `loadEnv()`
(`apps/api/src/config.ts`). Kansoku is deliberately fail-open: every key is
defaulted or optional and carries a warn-default policy, so a bad value warns
(an operator typo is never silently absorbed) and falls back to the default —
misconfiguration never crashes the observability service. The table below,
`apps/api/.env.example`, and `apps/api/turbo.json` are **generated** from the
spec — edit `src/env.ts` and run `npm run env:gen`; `npm run env:check` fails
on drift.

<!-- kagami-env:begin kansoku/api -->

<!-- Generated by @kagami/env — edit src/env.ts and run `npm run env:gen`. -->

| Var                              | Required | Default                                                   | Purpose                                                                                                                                                                                                                  |
| -------------------------------- | -------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `KANSOKU_HOST`                   | no       | `127.0.0.1`                                               | Standalone fallback bind host.                                                                                                                                                                                           |
| `PORT`                           | no       | `7779`                                                    | Standalone fallback bind port (Portless injects its own otherwise).                                                                                                                                                      |
| `LOG_LEVEL`                      | no       | `info`                                                    | Pino log level (`silent` in tests).                                                                                                                                                                                      |
| `MONGODB_URI`                    | no       | `mongodb://127.0.0.1:27017/kansoku?directConnection=true` | Connection URI for the storage layer.                                                                                                                                                                                    |
| `KANSOKU_INGEST_TOKEN`           | no       | —                                                         | Shared HMAC token presented by sibling shippers in the `x-kansoku-auth` header.                                                                                                                                          |
| `KANSOKU_LOGS_TTL_DAYS`          | no       | `30`                                                      | Time-series TTL for the `logs` collection, in days.                                                                                                                                                                      |
| `KANSOKU_ERRORS_TTL_DAYS`        | no       | `90`                                                      | TTL for the fingerprinted `errors` registry, in days (TTL index on `errors_last_seen`).                                                                                                                                  |
| `KANSOKU_MAX_META_COMBOS`        | no       | `1000`                                                    | Budget on distinct {service,component,env,level} metaField tuples tracked for the time-series collection.                                                                                                                |
| `KANSOKU_ALERT_WEBHOOK_URL`      | no       | —                                                         | Optional webhook fired when a brand-new error fingerprint shows up AND when an existing fingerprint spikes past KANSOKU_SPIKE_THRESHOLD within KANSOKU_SPIKE_WINDOW_MINUTES (subject to KANSOKU_SPIKE_COOLDOWN_MINUTES). |
| `KANSOKU_SPIKE_THRESHOLD`        | no       | `10`                                                      | Spike fires when this many occurrences of the same fingerprint land inside the window.                                                                                                                                   |
| `KANSOKU_SPIKE_WINDOW_MINUTES`   | no       | `5`                                                       | Rolling window for the spike counter, in minutes.                                                                                                                                                                        |
| `KANSOKU_SPIKE_COOLDOWN_MINUTES` | no       | `60`                                                      | Minimum gap between spike alerts for the same fingerprint.                                                                                                                                                               |

<!-- kagami-env:end kansoku/api -->

Notes:

- `LOG_PRETTY` _(unset → TTY-detect)_ is the `@kagami/logger` console gate (every service, Kansoku included): `1`/`true` forces human-pretty, `0`/`false` forces raw NDJSON; unset → pretty only when stdout is an interactive TTY. A runner-level knob, not part of the schema — `dev-all.sh` exports `LOG_PRETTY=1` for all dev children.
- `KANSOKU_INGEST_TOKEN` is operationally **required** for ingest — when unset, `POST /v1/logs` returns 503 fail-closed (the service won't silently accept unauthenticated ingest) and boot logs a warning.
- The spike window has fixed-window semantics: the first occurrence starts the window; subsequent ones increment until `now - windowStart > windowMinutes`, then the window resets. Threshold 1 is unreachable on first sighting (the new-error path returns before spike eval), hence the floor of 2.
- Both TTLs are capped at 365 days at apply time (longer retention degrades time-series bucket compaction).

## Dashboard (`apps/dashboard/.env`)

| Var               | Default                         | Purpose                                                                                                                            |
| ----------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `KANSOKU_API_URL` | `https://api.kansoku.localhost` | API base. Must be reachable from both the Next.js server _and_ the user's browser (the live-tail `EventSource` runs browser-side). |

## Sibling services

Every sibling that ships logs reads two vars:

| Var                    | Notes                                                                                                          |
| ---------------------- | -------------------------------------------------------------------------------------------------------------- |
| `KANSOKU_URL`          | Defaults to `https://api.kansoku.localhost`.                                                                   |
| `KANSOKU_INGEST_TOKEN` | Must match Kansoku's value; either-missing leaves the logger stdout-only (graceful in dev and during outages). |

Logger wrappers normalize empty strings to "missing" so a `KANSOKU_URL=`
(no value) typo doesn't silently disable the shipper.

## Token rotation

There is no live token rotation primitive. To rotate:

1. Generate a new token: `openssl rand -hex 32`
2. Update `KANSOKU_INGEST_TOKEN` in Kansoku's `.env`
3. Update `KANSOKU_INGEST_TOKEN` in every producer `.env` that ships to Kansoku
4. Restart all services (in any order — `dev-all.sh` boots them in parallel)

During the gap between (2) and (4) the shippers may briefly fail-open
into their local ring buffers — by design.

## Token / read-route auth model

`POST /v1/logs` is the only token-gated endpoint. All read routes
(`/v1/logs`, `/v1/tail`, `/v1/traces/:id`, `/v1/errors`, `/v1/services`)
and the meta routes (`/health`, `/ready`, `/version`) are
unauthenticated, matching the single-user-localhost convention from
the sibling APIs. The OS user is the trust boundary; Portless binds
loopback only.

## Mongo retention

The `logs` time-series collection has a TTL set via `expireAfterSeconds`
on creation, reconciled on every startup via `collMod`. The `errors`
registry has a TTL too — a single-field TTL index on `errors_last_seen`
(`KANSOKU_ERRORS_TTL_DAYS`, default 90). It expires per row off `lastSeen`,
so only fingerprints that have gone quiet age out; `recentTraceIds` stays
bounded per row regardless. `ensureIndexes` creates the TTL index, or
reconciles a pre-existing non-TTL `errors_last_seen` in place via
`collMod` (IndexOptionsConflict path).

Older `mongodb-memory-server` builds reject `collMod` on time-series
collections with code 167; the reconciler logs a warning and leaves the
existing TTL in place.
